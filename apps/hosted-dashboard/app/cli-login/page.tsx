// /cli-login placeholder. Implemented in Pillar 6.
// Callback target for `onemem login` from the CLI. Generates delegate key, POSTs to localhost:PORT/callback.
// Spec: docs/05-our-architecture/05-cli/login-flow.md + docs/05-our-architecture/06-dashboard/hosted-deploy.md
export default function CliLoginPage() {
  return <main>CLI login (skeleton)</main>;
}
