// Landing-shell placeholder for app.onemem.ai. Implemented in Pillar 6.
// Visitors with no session → see sign-in CTA; visitors with session → redirect to /dashboard.
export default function HostedRoot() {
  return <main>OneMem hosted (skeleton)</main>;
}
