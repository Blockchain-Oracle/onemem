{
  "name": "@onemem/oc-onemem",
  "version": "0.1.0",
  "description": "OneMem plugin for OpenClaw — adds verifiable on-chain action traces on top of oc-memwal",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "echo \"@onemem/oc-onemem: build stub — real build added when this packages pillar lands\"",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check ."
  },
  "dependencies": {
    "@onemem/sdk-ts": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
