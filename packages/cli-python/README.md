# onemem-cli (Python)

Python build of the OneMem CLI. Use `pipx install onemem-cli` for a global install. Exposes `onemem-py` (the TS CLI exposes `onemem`; both coexist).

See `docs/05-our-architecture/05-cli/command-surface.md`.
