"""onemem-cli — OneMem Python CLI.

A read-only mirror of the canonical TS `@onemem/cli` (verify / trace / health)
for Python-first environments that prefer `pip install` over `npm install -g`.
Installs the `onemem-py` command.
"""

__version__ = "0.1.0"
