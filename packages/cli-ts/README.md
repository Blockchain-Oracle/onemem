# @onemem/cli

The `onemem` command-line tool. Login, mint namespaces, launch the local dashboard, inspect traces.

See `docs/05-our-architecture/05-cli/command-surface.md` for the full command spec.
