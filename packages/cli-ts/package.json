{
  "name": "@onemem/cli",
  "version": "0.6.3",
  "description": "OneMem CLI — store and recall decentralized agent memory from the terminal",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "onemem": "./bin/onemem"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@onemem/sdk-ts": "workspace:*",
    "@mysten/sui": "^2.17.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
