# @onemem/openai-agents

OneMem provider for the OpenAI Agents SDK (TypeScript). A Python sibling lives at the same path under `pyproject.toml` if/when the OpenAI Agents Python SDK ecosystem matures.

See `docs/05-our-architecture/04-frameworks/openai-agents-provider.md`.
