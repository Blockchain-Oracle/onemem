{
  "name": "@onemem/openai-agents",
  "version": "0.1.0",
  "description": "OneMem provider for the OpenAI Agents SDK — verifiable memory + trace for OpenAI Agents",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "echo \"@onemem/openai-agents: build stub — real build added when this packages pillar lands\"",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check ."
  },
  "dependencies": {
    "@onemem/sdk-ts": "workspace:*"
  },
  "peerDependencies": {
    "@openai/agents": "^0.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
