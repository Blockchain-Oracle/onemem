[tools]
node = "20.18.0"
pnpm = "10.33.0"
python = "3.12"
uv = "latest"
rust = "stable"
# sui = "latest"   # mise does not currently ship a first-party sui plugin; install via brew (`brew install sui`) or release binaries

[env]
ONEMEM_REPO_ROOT = "{{config_root}}"
