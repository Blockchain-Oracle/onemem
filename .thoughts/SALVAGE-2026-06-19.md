# Salvage / Seed — pre-reset facts worth keeping (2026-06-19)

The full pre-reset working tree + the entire old `.thoughts/` (357 files) are preserved in **git commit `8a50980`** on branch `pillar-3-plugins` ("checkpoint: preserve pre-reset working tree"). Recover anything with `git show 8a50980:<path>`.

## Live deployments (we keep these)
- `https://onemem.xyz` — landing (Vercel `dpl_7QVMXfRcGiH4nTus31KWUBCYNbHK`)
- `https://app.onemem.xyz` — hosted dashboard (Vercel `dpl_BtEdipDexSrxYPgYniqWiJ7ZGvU7`)
- `https://docs.onemem.xyz` — docs (Vercel `dpl_F4iKnanzYDEq968cbtq1Z3hHNwLb`)

## Operational facts
- Sui testnet faucet: `POST https://faucet.testnet.sui.io/v2/gas` (the CLI faucet was dead).
- Local credentials: `~/.onemem/credentials.json` (MemWal delegate key + account id); MemWal config also in the root `.env`.
- WAL funding helper: `scripts/get-wal.ts` (SUI→WAL swap on testnet).

## Old trace-era on-chain (being REMOVED — reference only)
- OneMem custom Move package (testnet): `0xc2e8…f138` (upgraded), original `0x64c1…eafc`, registry `0x3c78…16e0`. This is the **trace** contract; the reset deletes `contracts/onemem`. Memory now lives on MemWal's contract, not this.
- Numerous old TraceSession / namespace / capability demo object IDs live in commit `8a50980` if ever needed.
