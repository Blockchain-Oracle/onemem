#!/usr/bin/env bash
# Initial publish of contracts/onemem to a target Sui network.
#
# Usage: bash scripts/deploy-contract.sh [testnet|mainnet]
# Spec: docs/05-our-architecture/00-overview/MONOREPO_LAYOUT.md scripts section.
#
# Skeleton — implemented in Pillar 1.

set -euo pipefail

NETWORK="${1:-testnet}"
echo "deploy-contract: skeleton (network=$NETWORK) — implemented in Pillar 1"
exit 0
