[package]
name = "nautilus-relayer"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "OneMem Nautilus TEE relayer (Pillar 12 stretch)"

[dependencies]
# Filled in if/when Pillar 12 ships. Skeleton only at v0.1.

[[bin]]
name = "nautilus-relayer"
path = "src/main.rs"
